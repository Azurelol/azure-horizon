<section class="tab {{tab.id}} {{tab.cssClass}} ah-application__browser__content"
         data-tab="{{tab.id}}" data-group="primary">
  {{#each tables as |table|}}
    {{{table}}}
  {{/each}}
</section>
