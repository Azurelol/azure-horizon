<section class="ah-chat-section">
  <div class="ah-chat-section__check__details__group">
    <div class="ah-chat-section__check__total --damage">
      <i class="ah-icon --s {{ahIconClass 'applyDamage'}}"></i>
      <span>{{localize 'AH.CHECK.TargetResource' result=damage.resolved.total resource='HP'}}</span>
    </div>

    {{#with damage.resolved}}
    <div class='ah-chat-section__check__components'>
        {{#each instances as |component|}}
          <div class="ah-chat-section__damage__component" data-tooltip="{{localize component.tooltip}}">
            {{ahInlineProperty 'damageTypes' component.type value=component.amount }}
          </div>
        {{/each}}
        {{#if modifiers}}
          {{#each modifiers as |mod|}}
            <div class="ah-chat-section__damage__component" data-tooltip="{{mod.key}}">
              {{ahInlineProperty 'damageTypes' mod.key }}
              {{#if mod.additive}}
                <span class="ah-inline-property__value">{{numberFormat mod.additive sign=true}}</span>
              {{/if}}
              {{#if (ahNotEquals mod.multiplicative 1)}}
                <span class="ah-inline-property__value">{{ahPercentModifier mod.multiplicative}}</span>
              {{/if}}
            </div>
          {{/each}}
        {{/if}}
    </div>
    {{/with}}
  </div>
</section>
