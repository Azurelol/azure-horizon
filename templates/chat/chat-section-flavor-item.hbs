<header class="ah-chat-section__flavor">
  {{ahDocumentAnchor item}}
  <span class="ah-chat-section__flavor__name">{{item.name}}</span>
  {{#each linked as |li|}}
    {{ahDocumentAnchor li classes="ah-image__link"}}
  {{/each}}
  {{#if subtitle}}
    <h3>{{localize subtitle}}</h3>
  {{/if}}
</header>

