<header class="ah-chat-section__flavor">
  {{#if icon}}
    <i class="ah-icon --s {{icon}}"></i>
  {{/if}}
  {{#if item}}
    {{ahDocumentAnchor item}}
  {{/if}}
  <span class="ah-chat-section__flavor__name">{{title}}</span>
</header>
