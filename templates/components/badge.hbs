<label class="ah-badge {{classes}}">
  <div>
    <i class="ah-icon --{{size}} {{iconClasses}} {{icon}}"></i>
    <span>{{ localize label }}</span>
  </div>
  {{#if path}}
    {{#if (eq type 'Number')}}
      <input type="Number" name="{{path}}" value="{{ value }}" min="{{min}}" max="{{max}}">
    {{/if}}

  {{else}}
    <input disabled value="{{ value }}">
  {{/if}}
</label>
