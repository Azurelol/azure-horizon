<a class="ah-button {{buttonClasses}}" data-action="{{action}}"
   {{dataattributes}}
   data-tooltip="{{localize tooltip}}">
  <i class="ah-icon --{{size}} {{icon}} {{iconClasses}}"></i>
  {{#if label}}
    <label>{{localize label}}</label>
  {{/if}}
</a>
