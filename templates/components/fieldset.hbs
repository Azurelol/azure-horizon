<fieldset class="ah-fieldset --grid  {{class}}">
  <legend>
    <label>
      {{#if icon}}<i class="ah-icon --s {{icon}}"></i>{{/if}}
      {{localize label}}
    </label>
  </legend>
  <div>
    {{> @partial-block}}
  </div>
</fieldset>
