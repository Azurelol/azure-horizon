<label class="ah-input {{classes}}" data-tooltip="{{localize tooltip}}" aria-describedby="tooltip">
  <i class="{{icon}}"></i>
  <span class="ah-input__label">{{localize label}}</span>
  <input class="ah__input__control" name="{{path}}" value="{{ value }}" type="{{type}}" />
</label>
