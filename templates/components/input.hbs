<label class="ah-input" data-tooltip="{{localize tooltip}}" aria-describedby="tooltip">
  <i class="{{icon}}"></i>
  <span>{{localize label}}</span>
  <input class="" name="{{path}}" value="{{ value }}" type="{{type}}" />
</label>
