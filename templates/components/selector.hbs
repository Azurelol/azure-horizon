<label class="ah-selector">
  <span class="ah-selector__label">{{localize label}}</span>
  <i class="ah-icon --s {{icon}}"></i>
  <select class="ah-selector__input" name="{{path}}">
    {{selectOptions values selected=value localize=true}}
  </select>
</label>
