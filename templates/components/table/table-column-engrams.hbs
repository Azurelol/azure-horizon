<td class="ah-table-column__engrams">
  {{#each system.slots.entries}}
    <adiv class="ah-character__engrams__slot">
      {{#if entry.item}}
      {{else}}
      {{/if}}
    </adiv>
  {{/each}}
</td>
