<td class="ah-table-column__engrams">
  {{#each system.slots.entries as |entry|}}
    <div class="ah-character__engrams__slot">
      {{#if entry.item}}
        <img src="{{entry.item.img}}">
      {{/if}}
    </div>
  {{/each}}
</td>
