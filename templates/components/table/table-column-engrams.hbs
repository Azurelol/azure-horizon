<td class="ah-table-column__engrams">
  {{#each system.slots.entries}}
    <div class="ah-table-column__engrams__slot">
      {{#if entry.item}}
      {{else}}

      {{/if}}
    </div>
  {{/each}}
</td>
