<section class="tab overview {{tab.cssClass}}" data-tab="overview" data-group="primary">
	<section class="party">
		{{#if characters}}
      {{{overview}}}
    {{else}}
      <div class="ah-message__centered">
        {{localize 'AH.SHEET.MESSAGE.NoDataAvailable'}}
      </div>
		{{/if}}
	</section>
</section>
